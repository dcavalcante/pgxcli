# yaml-language-server: $schema=https://goreleaser.com/static/schema.json
version: 2

project_name: pgxcli

release:
  draft: true
  prerelease: auto
  name_template: "pgxcli {{.Version}}"
  header: |
    # pgxcli {{.Version}}
  footer: |
    **Full Changelog**: https://github.com/balajz/pgxcli/blob/main/CHANGELOG.md

before:
  hooks:
    - go mod tidy

builds:
  - id: linux
    main: .
    binary: pgxcli
    goos: [linux]
    goarch: [amd64, arm64]
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
      - -X github.com/balajz/pgxcli/internal/version.Version={{.Version}}
      - -X github.com/balajz/pgxcli/internal/version.Commit={{.Commit}}
      - -X github.com/balajz/pgxcli/internal/version.BuildTime={{.Date}}

  - id: darwin
    main: .
    binary: pgxcli
    goos: [darwin]
    goarch: [amd64, arm64]
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
      - -X github.com/balajz/pgxcli/internal/version.Version={{.Version}}
      - -X github.com/balajz/pgxcli/internal/version.Commit={{.Commit}}
      - -X github.com/balajz/pgxcli/internal/version.BuildTime={{.Date}}

  - id: windows
    main: .
    binary: pgxcli
    goos: [windows]
    goarch: [amd64]
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
      - -X github.com/balajz/pgxcli/internal/version.Version={{.Version}}
      - -X github.com/balajz/pgxcli/internal/version.Commit={{.Commit}}
      - -X github.com/balajz/pgxcli/internal/version.BuildTime={{.Date}}

archives:
  - id: linux-archive
    ids: [linux]
    formats: [tar.gz]
    name_template: "pgxcli_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    wrap_in_directory: true
    files:
      - LICENSE
      - README.md

  - id: darwin-archive
    ids: [darwin]
    formats: [tar.gz]
    name_template: "pgxcli_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    wrap_in_directory: true
    files:
      - LICENSE
      - README.md

  - id: windows-archive
    ids: [windows]
    formats: [zip]
    name_template: "pgxcli_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    wrap_in_directory: false
    files:
      - LICENSE
      - README.md

nfpms:
  - ids: [linux]
    vendor: "Balaji J"
    maintainer: "Balaji J <balaji.jothi.in@gmail.com>"
    description: "An interactive PostgreSQL CLI written in Go"
    homepage: "https://github.com/balajz/pgxcli"
    license: "MIT"
    bindir: /usr/bin
    formats:
      - deb
      - rpm
      - apk
      - archlinux

checksum:
  name_template: "checksums.txt"

changelog:
  disable: true

aurs:
  - name: pgxcli-bin
    homepage: "https://github.com/balajz/pgxcli"
    description: "An interactive PostgreSQL CLI written in Go"
    maintainers:
      - "Balaji J <balaji.jothi.in@gmail.com>"
    license: "MIT"
    private_key: "{{ .Env.AUR_SSH_PRIVATE_KEY }}"
    git_url: "ssh://aur@aur.archlinux.org/pgxcli-bin.git"
    provides:
      - pgxcli
    conflicts:
      - pgxcli
    package: |-
      # navigate into the extracted archive directory
      cd pgxcli_*

      # bin
      install -Dm755 "./pgxcli" "${pkgdir}/usr/bin/pgxcli"

      # license
      install -Dm644 "./LICENSE" "${pkgdir}/usr/share/licenses/pgxcli/LICENSE"

dockers_v2:
  - images:
      - "ghcr.io/balajz/pgxcli"
      - "balajz/pgxcli"
    tags:
      - "latest"
      - "v{{ .Version }}"
    platforms:
      - linux/amd64
      - linux/arm64
